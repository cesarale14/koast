// Phase 2: Claude API integration for AI-powered guest messaging
// TODO: Implement when guest messaging feature begins

export async function generateGuestReply(
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  conversationHistory: { role: string; content: string }[],
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  propertyContext: { name: string; checkIn: string; checkOut: string }
): Promise<string> {
  throw new Error("Claude messaging not yet implemented — Phase 2");
}
